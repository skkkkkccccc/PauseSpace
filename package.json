{
  "name": "pausespace",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first, static, privacy-first scene-based attention-reset web app. Not diagnosis, treatment, counselling, or emergency support.",
  "type": "module",
  "scripts": {
    "test": "node --test src/test/smoke.test.mjs",
    "lint": "node src/tools/lint.mjs",
    "build": "node src/tools/build.mjs",
    "start": "node src/tools/serve.mjs",
    "preview": "npm run build && npm run start",
    "audio:dry-run": "node scripts/generate-audio.mjs --dry-run",
    "audio:generate": "node scripts/generate-audio.mjs",
    "test:audio": "node --test tests/audio-generation.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
