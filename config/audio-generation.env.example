# Placeholder-only development-time configuration.
# Copy approved values into the repository's ignored local .env file.
# Never put real credentials in this file.
TTS_PROVIDER=<approved-provider>
TTS_API_KEY=<set-locally-never-commit>
TTS_MODEL=<approved-model-id>
TTS_VOICE=<approved-voice-id>
TTS_LOCALE=<approved-locale>
TTS_MAX_REQUESTS=4
TTS_MAX_COST=<approved-hard-cap>
